#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t,n,x;
    cin>>t;
    while(t--)
    {
        cin>>n;
        vector<int>v;

        for(int i=0;i<n*2;i++)
        {
            cin>>x;
            v.push_back(x);
        }
       for(int i=0;i<v.size();i+=2)
       {
           cout<<v[0]
           if(v[])
       }
    }
    return 0;
}
